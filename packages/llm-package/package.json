{
  "name": "@mcp/llm-package",
  "version": "0.2.0",
  "description": "LLM integration & support package.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/bedrock-sdk": "*",
    "@anthropic-ai/vertex-sdk": "*",
    "axios": "*",
    "dotenv": "*",
    "openai": "*"
  },
  "peerDependencies": {
    "@anthropic-ai/sdk": "*"
  },
  "peerDependenciesMeta": {
    "@anthropic-ai/sdk": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
